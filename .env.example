# ============================================================
# Task Duck v3.1 — Environment Configuration
# ============================================================

# AUTH — Challenge-Response (Argon2id + nonce)
#
# Hash chain: sha256(password) → argon2id(5t, 128MB, 8p) → sha256 = verifier
# The password NEVER leaves the browser. Only a one-time proof hash is sent.
#
# Generate with:
#   node hash-password.js
#   docker compose run --rm task-duck node hash-password.js
PASSWORD_VERIFIER=

# Salt for Argon2 — must match between CLI and server
# Change this to something unique for your deployment
ARGON2_SALT=task-duck-v3-default-salt

# Session duration (hours)
SESSION_HOURS=24

# JWT signing secret (auto-generated if empty, set for persistence across restarts)
JWT_SECRET=

# ============================================================
# LLM PROVIDERS — add keys for providers you want
# ============================================================
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=

# ============================================================
# SENSITIVE DATA MASKING
# ============================================================
# Custom terms to mask before sending to LLMs
# Format: term1=PLACEHOLDER1,term2=PLACEHOLDER2
# Example: mycompany=COMPANY,prod-api.internal=SERVICE_A
CUSTOM_MASKS=

# ============================================================
# SERVER
# ============================================================
PORT=8080
