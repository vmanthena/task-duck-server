# ============================================================
# Task Duck — Environment Configuration
# ============================================================

# AUTH — Generate hash: curl -X POST http://localhost:3456/api/hash-password \
#   -H "Content-Type: application/json" -d '{"password":"your-password"}'
PASSWORD_HASH=

# Session duration in hours (default: 24)
SESSION_HOURS=24

# JWT signing secret (auto-generated if empty, but set one for persistence across restarts)
JWT_SECRET=

# ============================================================
# LLM PROVIDERS — Add keys for providers you want to use
# ============================================================

# Anthropic (Claude)
ANTHROPIC_API_KEY=

# OpenAI (GPT-4o / ChatGPT)
OPENAI_API_KEY=

# Google Gemini
GEMINI_API_KEY=

# ============================================================
# SENSITIVE DATA MASKING
# ============================================================
# Custom terms to always mask before sending to LLMs.
# Format: term1=PLACEHOLDER1,term2=PLACEHOLDER2
# Example: mycompany=COMPANY,prod-api.internal=SERVICE_A,custdb01=DATABASE_1
#
# Auto-detected patterns (always active):
#   - Email addresses, IP addresses, URLs
#   - API keys, UUIDs, database connection strings
#   - File paths, phone numbers, SSNs, AWS ARNs
#   - Kubernetes namespaces
CUSTOM_MASKS=

# ============================================================
# SERVER
# ============================================================
PORT=3000
