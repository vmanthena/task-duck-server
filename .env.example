# ============================================================
# Task Duck v3.2 — Environment Configuration
# ============================================================

# AUTH — bcrypt challenge-response
# Chain: sha256(password) → bcrypt(salt) → sha256 = verifier
# Generate both with: npm run hash
BCRYPT_SALT=
PASSWORD_VERIFIER=

# bcrypt cost (min 15, max 16 — higher = slower + more secure)
BCRYPT_COST=15

# Session duration (hours)
SESSION_HOURS=24

# JWT secret (auto-generated if empty, set for persistence)
JWT_SECRET=

# ============================================================
# LLM PROVIDERS
# ============================================================
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=

# Optional: override default models
# ANTHROPIC_MODEL=claude-haiku-4-5-20251001
# GEMINI_MODEL=gemini-2.0-flash-lite

# ============================================================
# LOCAL LLM (Ollama)
# ============================================================
# Set base URL to enable Ollama. Default: http://localhost:11434
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=qwen2.5:7b

# Note: If no API keys are configured and no Ollama URL is set,
# a Mock (Dev) provider is automatically available for testing.

# ============================================================
# SENSITIVE DATA MASKING
# ============================================================
# Format: term1=PLACEHOLDER1,term2=PLACEHOLDER2
CUSTOM_MASKS=

# ============================================================
# SERVER
# ============================================================
PORT=8080

# Log level: debug | info | warn | error (default: info)
LOG_LEVEL=info
