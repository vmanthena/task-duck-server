# ============================================================
# Task Duck v3.2 — Environment Configuration
# ============================================================

# AUTH — bcrypt challenge-response
# Chain: sha256(password) → bcrypt(salt) → sha256 = verifier
# Generate both with: node hash-password.js
BCRYPT_SALT=
PASSWORD_VERIFIER=

# bcrypt cost (default 12, higher = slower + more secure)
BCRYPT_COST=20

# Session duration (hours)
SESSION_HOURS=24

# JWT secret (auto-generated if empty, set for persistence)
JWT_SECRET=

# ============================================================
# LLM PROVIDERS
# ============================================================
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=

# ============================================================
# SENSITIVE DATA MASKING
# ============================================================
# Format: term1=PLACEHOLDER1,term2=PLACEHOLDER2
CUSTOM_MASKS=

# ============================================================
# SERVER
# ============================================================
PORT=8080
