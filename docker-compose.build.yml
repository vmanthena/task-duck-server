## Use this for local builds: docker compose -f docker-compose.build.yml up -d
services:
  task-duck:
    build: .
    container_name: task-duck
    restart: unless-stopped
    ports:
      - "3456:3000"
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
